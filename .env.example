# Aptabase app key for anonymous, opt-out telemetry.
# Leave empty to disable telemetry entirely (the SDK is never initialized).
# Get a key at https://aptabase.com (free tier). Copy this file to .env.local and fill it in.
VITE_APTABASE_APP_KEY=
